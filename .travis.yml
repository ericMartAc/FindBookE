languaje: node_js
node_js: 
   - 12.18.0
cache: yarn

install:
  - yarn install
  - travis_wait 20 mvn install

script:
  - yarn start
  - yarn test
  - yarn run build 

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $BACKEND_CONFIG 
  keep_history: true
  on:
    branch: backend_config




  

